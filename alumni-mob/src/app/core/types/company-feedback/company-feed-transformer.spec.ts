import { CompanyFeedTransformer } from './company-feed-transformer';

describe('CompanyFeedTransformer', () => {
  it('should create an instance', () => {
    expect(new CompanyFeedTransformer()).toBeTruthy();
  });
});
